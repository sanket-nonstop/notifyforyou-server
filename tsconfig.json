{
  "compilerOptions": {
    // --- FILE STRUCTURE ---
    "rootDir": "./src", // Added — where TypeScript source files live
    "outDir": "./dist", // Added — where compiled JS should go

    // --- ENVIRONMENT SETTINGS ---
    "module": "commonjs", // Changed (was "nodenext") — Node backend uses CommonJS by default
    "target": "ES2020", // Changed (was "esnext") — stable modern JS target for Node 18+
    "lib": ["es2020"], // Added — ensures modern language features
    "types": ["node"], // Changed (was empty []) — load Node.js global types
    //   Also remember: npm install -D @types/node

    // ❌ Removed: "jsx": "react-jsx" (used for React frontends only)
    // ❌ Removed: "verbatimModuleSyntax" — not needed for backend
    // ❌ Removed: "noUncheckedSideEffectImports" — experimental & unnecessary here
    // ❌ Removed: "moduleDetection": "force" — default "auto" works fine for backend

    // --- OUTPUT CONFIG ---
    "sourceMap": true, // Keep — helps with debugging
    "declaration": true, // Keep — generates .d.ts type files (useful for library exports)
    "declarationMap": true, // Keep — helpful for mapping declaration files

    // --- TYPE CHECKING OPTIONS ---
    "strict": true, // Keep — best practice
    "noUncheckedIndexedAccess": true, // Keep — safer indexing
    "exactOptionalPropertyTypes": true, // Keep — enforces explicit optional property types
    "esModuleInterop": true, // Added — allows default imports from CommonJS modules
    "resolveJsonModule": true, // Added — allows importing .json files

    // --- MISC / CLEANUP ---
    "skipLibCheck": true, // Keep — faster builds, ignore lib type checking
    "forceConsistentCasingInFileNames": true, // Added — avoid case mismatch issues
    "baseUrl": "./src", // Added — base directory for path aliases
    "paths": {
      // Added — clean import paths instead of ../../ hell
      "@config/*": ["config/*"],
      "@constants/*": ["constants/*"],
      "@core/*": ["core/*"],
      "@graphql/*": ["graphql/*"],
      "@logger/*": ["logger/*"],
      "@middlewares/*": ["middlewares/*"],
      "@models/*": ["models/*"],
      "@modules/*": ["modules/*"],
      "@rest/*": ["rest/*"],
      "@sockets/*": ["sockets/*"],
      "@utils/*": ["utils/*"]
    }
  },
  "include": ["src"], // Added — compile all files inside src/
  "exclude": ["node_modules", "dist"] // Added — don’t compile these

  // // Visit https://aka.ms/tsconfig to read more about this file
  // "compilerOptions": {
  //   // File Layout
  //   // "rootDir": "./src",
  //   // "outDir": "./dist",

  //   // Environment Settings
  //   // See also https://aka.ms/tsconfig/module
  //   "module": "nodenext",
  //   "target": "esnext",
  //   "types": [],
  //   // For nodejs:
  //   // "lib": ["esnext"],
  //   // "types": ["node"],
  //   // and npm install -D @types/node

  //   // Other Outputs
  //   "sourceMap": true,
  //   "declaration": true,
  //   "declarationMap": true,

  //   // Stricter Typechecking Options
  //   "noUncheckedIndexedAccess": true,
  //   "exactOptionalPropertyTypes": true,

  //   // Style Options
  //   // "noImplicitReturns": true,
  //   // "noImplicitOverride": true,
  //   // "noUnusedLocals": true,
  //   // "noUnusedParameters": true,
  //   // "noFallthroughCasesInSwitch": true,
  //   // "noPropertyAccessFromIndexSignature": true,

  //   // Recommended Options
  //   "strict": true,
  //   "jsx": "react-jsx",
  //   "verbatimModuleSyntax": true,
  //   "isolatedModules": true,
  //   "noUncheckedSideEffectImports": true,
  //   "moduleDetection": "force",
  //   "skipLibCheck": true,
  // }
}
